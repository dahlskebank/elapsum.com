# Deploy secrets + lock (deploy.sh reads .env; .env.example is the committed template)
.env
.deploy.lock

# Daniel's real Days Counter backup — personal event data, never commit
_temp/backup*

# Intermediate captures from the _dev asset generators (icon-tile/og-image)
_dev/*.png
